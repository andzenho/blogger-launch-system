---
description: Вызвать orchestrator — показать текущую фазу, статус и следующий шаг.
---

Вызови `orchestrator` через Task tool. Задача:
- Прочитай `state.md` и `CLAUDE.md` текущего проекта
- Проверь свежесть `brief/`
- Доложи в формате 4 строк: фаза / статус / следующий шаг / команда

Работай из папки текущего проекта. Если сессия открыта из корня репо — скажи продюсеру перейти в `projects/blogger-[имя]/`.
